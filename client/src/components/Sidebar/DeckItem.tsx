import React from 'react';
import styled from 'styled-components';

const SelectedCardWrapper = styled.div`
  height: 80px;
  border-bottom: solid 1px black;
`;

export default () => {
  return <SelectedCardWrapper />;
};
