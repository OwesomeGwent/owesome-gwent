import AppBar from '@material-ui/core/AppBar';
import Toolbar from '@material-ui/core/Toolbar';
import React from 'react';
const Header = () => {
  return (
    <AppBar position="sticky">
      <Toolbar variant="dense">오우썸 궨트</Toolbar>
    </AppBar>
  );
};

export default Header;
